--- src/App.svelte
+++ src/App.svelte
@@ -446,6 +446,12 @@
         title={routeToastCopy.title}
         detail={routeToastCopy.detail}
         hint={routeToastCopy.hint}
+        actionLabel={routeToastCopy.actionLabel}
+        onAction={routeToastCopy.hasAction ? () => {
+          app.settingsOpen = true;
+          app.settingsInitialTab = "interface";
+        } : undefined}
         onDismiss={() => micToast.dismiss()}
       />
     </div>
